interface:
  display_name: "To Tickets"
  short_description: "Workflow guidance for to tickets"
  default_prompt: "Use $to-tickets for this repository workflow task."
policy:
  allow_implicit_invocation: false
